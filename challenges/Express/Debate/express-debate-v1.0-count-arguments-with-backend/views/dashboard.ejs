<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Debate</title>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="stylesheet" type="text/css" href="css/style.css">

</head>
<body>
    <div class="container">
        <h1>Hello, <%= user[0].name %>!!</h1>
        <a href="/logout" class="logout">Logout</a>
        <% if(messages.messages) { %>
		     <% for (var message of messages.messages) { %>
		     <div class="alert-danger" role="alert"><%= message %></div>
		     <% } %>
		 <% } %>
        <form method="post" action="/create-topic">
            <label for="title">Topic: </label>
            <input type="text" name="title" id="title" placeholder="ex. Chicken or Egg?">          
            <label for="description">Description: </label>
            <textarea name="description" rows="5"></textarea>
            <button type="submit">Post</button>
        </form>
        <h5>Debate topics created by other users:</h5>
        <table class="table other-topics">
            <thead> 
                <tr>
                    <th>Topic</th>
                    <th>Date Created</th>
                    <th>Arguments</th>
                    <th>Leading Stance</th>
                </tr>
            </thead>
            <% if(fresh_topics) { %>
            <tbody>
            	<% for (var fresh_topic of fresh_topics) {%>
                <tr>
                    <td><a class="fresh-topic" href="/topics/<%= fresh_topic.id  %>"><%= fresh_topic.title %></td>
                    <td><%= fresh_topic.created_at.toDateString() %></td>
                    <td class="fresh-count"><%=fresh_topic.point %></td>
                    <td><%= fresh_topic.stance %></td>
                </tr>
                 <% } %>
            </tbody>
            <% } %>
        </table> 
        <h5>Debate topics I created:</h5>
        <table class="table my-topics">
            <thead>
                <tr>
                    <th>Topic</th>
                    <th>Date Created</th>
                    <th>Arguments</th>
                    <th>Leading Stance</th>
                </tr>
            </thead>
            <% if(my_topics) { %>
           
            <tbody>
                <% for (var my_topic of my_topics) {%>
                <tr>
                    <td><a class="fresh-topic" href="/topics/<%= my_topic.id  %>"><%= my_topic.title %></td>
                    <td><%= my_topic.created_at.toDateString() %></td>
                    <td><%= my_topic.point %></td>
                    <td><%= my_topic.stance %></td>
                </tr>
                <% } %>
            </tbody>
            <% } %>
        </table> 
    </div>
</body>
</html>

